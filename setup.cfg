[options]
packages = find:
namespace_packages = ckanext
install_requires =
include_package_data = True

[options.packages.find]
include =
    ckanext
    ckanext.*

[tool:pytest]
testpaths = ckanext/cwbi_harvesters/tests
